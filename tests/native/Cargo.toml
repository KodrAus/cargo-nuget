# NOTE: changing [package] values will break tests

[package]
name = "native_test"
version = "0.0.1"
authors = ["Somebody", "Somebody Else"]
repository = "http://examplerepository.com"
description = "A library for testing cargo nuget"

[lib]
crate-type = ["cdylib"]

[dependencies]
