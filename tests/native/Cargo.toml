# Changes here will break tests

[package]
name = "native_test"
version = "0.0.4"
authors = ["Somebody", "Somebody Else"]
description = "A library for testing cargo nuget"

[lib]
crate-type = ["dylib"]

[dependencies]
